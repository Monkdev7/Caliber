// Email sending is disabled for now.
// These no-op exports keep the import surface stable if needed later.
export const sendWelcomeEmail = async () => undefined;
export const sendResetEmail = async () => undefined;
